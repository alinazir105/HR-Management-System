{
  "name": "hr-management-system",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "concurrently \"cd frontend && npm install\" \"cd backend && npm install\"",
    "start": "concurrently \"cd frontend && npm run dev\" \"cd backend && cd src && nodemon server.js\"",
    "backend": "cd backend && nodemon server",
    "frontend": "cd frontend && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.9.0",
    "concurrently": "^9.1.2",
    "date-fns": "^3.6.0",
    "hr-management-system": "file:",
    "multer": "^1.4.5-lts.2",
    "react-day-picker": "^9.6.7"
  }
}
